# Agent Memory & Context

## Critical User Preferences
- **Version Control Rules**: DO NOT commit to git or push to origin without explicit user permission. Always ask before running `git commit` or `git push`.
